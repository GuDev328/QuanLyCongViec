<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuanLyCongViec"
        tools:targetApi="31">
        <activity
            android:name=".TaskAction.TaskDoneView"
            android:exported="false"
            android:label="Chi tiết công việc"
            android:theme="@style/CustomDialogTheme" />
        <activity
            android:name=".TaskAction.TaskActionView"
            android:exported="false"
            android:label="Chi tiết công việc"
            android:theme="@style/CustomDialogTheme" />
        <activity
            android:name=".TaskAction.TaskActionAdd"
            android:exported="false"
            android:label="Tạo công việc"
            android:theme="@style/CustomDialogTheme" />
        <activity
            android:name=".Auth.ForgotPassword"
            android:exported="false" />
        <activity
            android:name=".Auth.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".Auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>